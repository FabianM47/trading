/**
 * Auth Layout
 * 
 * Minimal layout for authentication pages (signin, verify-request)
 * No header, no navigation - just clean auth UI
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
